import AnalyzerSection from "../components/AnalyzerSection";

export default function AppDashboard() {
  return (
    <main className="relative bg-transparent z-10 w-full min-h-screen flex flex-col overflow-x-hidden pt-12 text-slate-800">
      <AnalyzerSection />
    </main>
  );
}
